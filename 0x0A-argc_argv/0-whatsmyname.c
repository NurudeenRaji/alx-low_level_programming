#include <stdio.h>

/**
 * main - Entry point.
 * @argc: argument count.
 * @argv: argument array.
 * Return: 0 (Success), 1 (Error).
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
