#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two integers
 * @a: first param.
 * @b: second param
 * Return: the result.
 */

int mul(int a, int b)
{
	int result;

	result  = a * b;

	return (result);
}
